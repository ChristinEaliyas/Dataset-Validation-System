<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="home.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Data Validation</title>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-6 p-4">
                <h1 id="main-heading">DataSet Validation System</h1>
            </div>
            <div class="col-lg-4"></div>
            <% if (session.user_id) { %>
                <div class="col-lg-2 p-4 routes-links">
                    <a href="/logout" class="text-decoration-none text-white">Logout</a>
                </div>
            <% } else { %>
                <div class="col-lg-1 p-4">
                    <a href="/register" class="text-decoration-none text-white">Sign Up</a>
                </div>
                <div class="col-lg-1 p-4">
                    <a href="/login" class="text-decoration-none text-white">Sign In</a>
                </div>
            <% } %>
        </div>
    </div>
    <div class="container-fluid">
        <div class="col-lg-10 data-content">
            <div class="row">
                <div class="data-field">
                    <textarea id="data_1" class="form-control" rows="3" readonly><%= data_1 %></textarea>
                </div>
                <div class="data-field">
                    <textarea id="data_2" class="form-control" rows="3" readonly><%= data_2 %></textarea>
                </div>
                <div class="col-lg-9">
                    <div class="input-buttons">
                        <form action="/" method="POST">
                            <input type="hidden" id="record_id" name="record_id" value="<%= record_id %>">
                            <button type="submit" name="is_correct" value="true">Correct</button>
                            <button type="submit" name="is_correct" value="false">Incorrect</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
